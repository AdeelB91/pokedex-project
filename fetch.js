import { print as p } from './utils.js';

// async function fetchPokeUrl(dataUrl) {
//   try {
//       dataUrl.forEach(entry => {
//       const response = await fetch(entry);
//       p('Response:');
//       p(response);
//       const data = await response.json();
//       p('Data:');
//       p(data);
//     } catch (error) {
//       console.log(error);
//     }

//   })
// }
